## 2024-05-18 - Added loading spinners to dialogs
**Learning:** Loading spinners can be easily added to Shadcn UI buttons by rendering a Lucide React icon with the `animate-spin` utility class conditionally based on `pending` state from `useTransition`.
**Action:** Apply this pattern to other async submit buttons.
