{
  "name": "@vooster/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "vspec": "./bin/run.js"
  },
  "scripts": {
    "build": "cd ../.. && tsc -p tsconfig.json",
    "test": "cd ../.. && vitest run apps/cli/tests",
    "typecheck": "cd ../.. && tsc -p tsconfig.json --noEmit"
  }
}
