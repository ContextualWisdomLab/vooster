{
  "name": "@vooster/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"api shell: build waits for relocation\"",
    "test": "echo \"api shell: tests wait for relocation\"",
    "typecheck": "echo \"api shell: typecheck waits for relocation\""
  }
}
