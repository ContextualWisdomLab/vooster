## 2024-06-01 - Added loading spinner to async buttons
**Learning:** Adding a small `Loader2` spinner icon to async buttons improves the UX.
**Action:** When working on async forms, add a `Loader2` spinner from `lucide-react` to the submit button while the form is pending.
